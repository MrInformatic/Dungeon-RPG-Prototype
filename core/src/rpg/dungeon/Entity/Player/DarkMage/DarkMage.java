package rpg.dungeon.Entity.Player.DarkMage;

import com.badlogic.gdx.graphics.Color;
import com.badlogic.gdx.maps.tiled.TiledMap;
import com.badlogic.gdx.physics.box2d.World;
import rpg.dungeon.Entity.Player.Player;

/**
 * Created by philipp on 17.03.15.
 */
/*public class DarkMage extends Player {
    public DarkMage(TiledMap map, World world) {
        //super(map, world, new Color(0.5f,0f,1f,1f));
    }

    @Override
    public void dispense(int value) {
        if(value == 0){
            pool.add(new BlackHole(sprite.getX()+12f,sprite.getY()+12));
        }
        if(value == 1){
            pool.add(new WhiteHole(sprite.getX()+12f,sprite.getY()+12));
        }
        if(value == 3){
            pool.add(new MagicPoison(sprite.getX()+12f,sprite.getY()+12));
        }
    }

    @Override
    public void enddispense(int value) {

    }
}*/
